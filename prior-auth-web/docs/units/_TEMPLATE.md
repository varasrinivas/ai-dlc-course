# UW-XX — <one line, in the language of the business>

> One unit of work = one bolt = one session. If you can't finish it in a session, it's an
> epic wearing a costume — split it and say so.

## Intent

<Why this exists, stated the way the person who wants it would state it. Not a spec.>

## Acceptance criteria

1. <Testable. Each one should map to a test name.>
2. <...>
3. <Any criterion that came from a human decision cites it: (see docs/decisions.md#D-00X)>

## Out of scope

- <The thing a reader will assume is included. Say it isn't, and which bolt takes it.>

## Open questions — for humans, not for the engine

- <Anything the engine cannot derive from this repo. The bolt does not start until these
  are answered. If this section is empty, say so explicitly — an empty section is a claim.>

## Gates

- `npm test` green, output shown (not asserted)
- `npm run typecheck` clean
- decisions recorded in `docs/decisions.md` and cited from the code
