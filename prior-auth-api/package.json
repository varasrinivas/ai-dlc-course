{
  "name": "prior-auth-api",
  "version": "0.1.0",
  "private": true,
  "description": "Prior Auth Portal API \u2014 the practice repo for the AWS AI-DLC course. Seeded at the state just before the hello-world bolt.",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  }
}
