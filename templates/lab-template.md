# Lab {MODULE_ID} — {LAB_TITLE}

> Module: {MODULE_ID} — {MODULE_TITLE}
> Audience: {leader | practitioner | both} · Estimated time: {N} min
> Domain: Prior Auth Portal ({entities touched, e.g. AuthRequest, ClinicalCriteria})

## Path A — Understand It (no tooling required)

**Artifact:** {a plan file, Mob Elaboration transcript, diff, or steering file — inline or path}

**Trace it:**
1. {step}
2. {step}
3. {step}

**Check yourself:**
1. {question}
2. {question}
3. {question}

<details><summary>Answers</summary>

1. {answer}
2. {answer}
3. {answer}

</details>

## Path B — Build It with AI

> The unit of work: {one-sentence intent, phrased as business intent, not implementation}.
> Both variants below must produce the same artifact. Prompts model AI-DLC: state intent,
> ask the engine to plan first, checkpoint before code.

### Claude Code variant

```text
# Claude Code
{exact prompt or slash-command sequence — includes "plan first, wait for my approval"}
```

**Expected artifact:** {file(s)/behavior}
**Verify:** {command or observable check, PowerShell-safe}

### Codex CLI variant

```text
# Codex CLI
{exact prompt or command sequence — includes the same plan-first checkpoint}
```

**Expected artifact:** {same artifact as above}
**Verify:** {same check}

**Parity note:** {one sentence confirming both engines land on the same artifact, and any
expected surface differences (file layout, wording) that do not matter}

<!-- For leader-only modules, replace Path B above with: -->
## Path B — Lead It (decision exercise)

**Scenario:** {governance/rollout/metrics decision from the Prior Auth Portal program}
**Your task:** {draft the gate / choose the pilot / define the metric — a written artifact}
**Compare:** <details><summary>A worked answer</summary>{model answer}</details>

## Done when

- [ ] {observable criterion}
- [ ] {observable criterion}
- [ ] {observable criterion}
